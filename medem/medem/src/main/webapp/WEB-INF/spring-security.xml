<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">
 
    <!-- enable use-expressions -->
	<http auto-config="false" use-expressions="true">
 
		<intercept-url pattern="/admin**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/newUser**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/reportCharts**" access="hasRole('ROLE_ADMIN')"/>
		<intercept-url pattern="/editCompany**" access="hasRole('ROLE_ADMIN')" />
				
		<intercept-url pattern="/newEmployee**" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/employee**" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/workBackground**" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/familyHistory**" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/medicalHistory**" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/medicalExam**" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/indexEmployees**" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/newArea**" access="hasRole('ROLE_USER')"/>
		<intercept-url pattern="/newArea/**" access="hasRole('ROLE_USER')"/>
		<intercept-url pattern="/welcome**" access = "hasRole('ROLE_USER')" />
		<intercept-url pattern="/uploadFile**" access = "hasRole('ROLE_USER')" />
		<intercept-url pattern="/showCompany**" access = "hasRole('ROLE_USER')" />
		<intercept-url pattern="/newCompany**" access= "hasRole('ROLE_ADMIN')" />
 
		<!-- access denied page -->
		<access-denied-handler error-page="/403" />

		<form-login 
		    login-page="/login" 
		    default-target-url="/welcome" 
			authentication-failure-url="/login?error" 
			username-parameter="username"
			password-parameter="password" />
		
		
		<logout 
			logout-success-url="/login?logout"
			delete-cookies="JSESSIONID"  />
			
		<!-- enable csrf protection -->
		<csrf/>
		
<!-- 		<port-mappings> -->
<!--           Default ports -->
<!-- 	      <port-mapping http="80" https="443"/> -->
<!-- 		  Websphere default ports -->
<!-- 		  <port-mapping http="9080" https="9443"/> -->
<!-- 		  Tomcat default ports -->
<!-- 		  <port-mapping http="8080" https="8443"/> -->
<!-- 		  Jetty custom ports -->
<!-- 		  <port-mapping http="7777" https="7443"/> -->
<!-- 		</port-mappings>		 -->
    
    	<!-- This to prevent more than one session per user -->
      	<session-management>
          <concurrency-control max-sessions="1" error-if-maximum-exceeded="true" expired-url="/login" />
      	</session-management>

	</http>
	
 
	<!-- Select users and user_roles from database -->
	<authentication-manager>
	  <authentication-provider>
	    <!-- This encodes password provided to an md5 hash -->
	    <password-encoder hash="md5"/>
	    
		<jdbc-user-service data-source-ref="dataSource"
		  users-by-username-query=
		    "select username, password, enabled from users where username=?"
		  authorities-by-username-query=
		    "select username, role from user_roles where username =?  " />
	  </authentication-provider>
	</authentication-manager>
 
</beans:beans>